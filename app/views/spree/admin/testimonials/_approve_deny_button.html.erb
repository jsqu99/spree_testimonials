<% if action==:approve  %>
  <%= link_to_with_icon('add', t('spree.testimonials.approve'), approve_admin_testimonial_url(testimonial)) %>
<% else %>
  <%= link_to_with_icon('delete', t('spree.testimonials.revoke approval'), deny_admin_testimonial_url(testimonial)) %>
<% end %>
