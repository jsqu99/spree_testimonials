<% if action==:approve  %>
  <%=    link_to_with_icon('add', 'approve', approve_admin_testimonial_url(testimonial)) %>
<% else %>
  <%=    link_to_with_icon('delete', 'revoke approval', deny_admin_testimonial_url(testimonial)) %>
<% end %>
